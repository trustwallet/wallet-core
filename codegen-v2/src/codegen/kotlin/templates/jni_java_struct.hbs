// Copyright © 2017-{{current_year}} Trust Wallet.
//
// This file is part of Trust. The full Trust copyright notice, including
// terms governing use, modification, and redistribution, is contained in the
// file LICENSE at the root of the source code distribution tree.
//
// This is a GENERATED FILE, changes made here WILL BE LOST.
//

#include <jni.h>
#include <stdio.h>
{{#each includes}}
#include <{{name}}.h>
{{/each}}

#include <TrustWalletCore/TW{{class_name}}.h>

#include "TWJNI.h"
#include "{{class_name}}.h"

{{#each method}}
{{return.type}} JNICALL {{name}}({{#each params}}{{type}} {{name}}{{#unless @last}}, {{/unless}}{{/each}}) {
	{{return.type}} result = NULL;

	{{#each operations}}
	{{#if this.call}}
    {{this.type}} {{this.var_name}} = {{this.call}};
	{{/if}}
	{{#if this.call_optional}}
    {{this.type}} {{this.var_name}} = {{this.call}};
	if ({{this.call_optional.var_name}} == NULL) {
		{{#each each}}
		{{! Usually used for cleanups}}
		{{this.call}}
		{{/each}}
	}
	{{/if}}
	{{#if this.return}}
	return {{this.return.call}}
	{{/if}}
	{{/each}}
}
{{/each}}
