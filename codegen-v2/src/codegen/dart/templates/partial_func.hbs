    {{#if is_static}}static{{/if}} {{return.type}}{{#if return.is_nullable}}?{{/if}} {{#unless is_public}}_{{/unless}}{{name}}({{#each params}}{{type}} {{name}}{{#unless @last}}, {{/unless}}{{/each}}) {
    {{#each operations}}
        {{#if this.call}}
        final {{this.call.var_name}} = {{this.call.call}};
        {{/if}}
        {{#if this.call_optional}}
        final UnsafeRawPointer? ptr;
        if let {{this.call_optional.var_name}} = {{this.call_optional.var_name}} {
            ptr = {{this.call_optional.call}}
        } else {
            ptr = nil
        }
        {{#if this.call_optional.defer}}
        defer {
            if let {{this.call_optional.var_name}}  = ptr {
                {{this.call_optional.defer}}
            }
        }
        {{/if}}
        final {{this.call_optional.var_name}} = ptr;

        {{/if}}
        {{#if this.guarded_call}}
        guard let {{this.guarded_call.var_name}} = {{this.guarded_call.call}} else {
            return nil
        }
        {{/if}}
        {{#if this.defer_call}}
        {{#if this.defer_call.call}}
        {{this.defer_call.call}};
        {{/if}}

        {{/if}}
        {{#if this.return}}
        return {{this.return.call}};
        {{/if}}
    {{/each}}
    }
